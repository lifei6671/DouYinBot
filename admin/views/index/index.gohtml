
<!doctype html>
<html lang="zh-CN" class="h-100">
<head>
    <meta name="referrer" content="never">
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="抖音无水印工具_最新抖音在线无水印解析">
    <title>抖音无水印工具_最新抖音在线无水印解析</title>
    <link href="/static/bootstrap/v4.6.0/css/bootstrap.min.css" rel="stylesheet">
    <!-- Favicons -->
    <link rel="icon" href="/static/favicons/favicon.ico">

    <style>
        .bd-placeholder-img {
            font-size: 1.125rem;
            text-anchor: middle;
            -webkit-user-select: none;
            -moz-user-select: none;
            -ms-user-select: none;
            user-select: none;
        }

        @media (min-width: 768px) {
            .bd-placeholder-img-lg {
                font-size: 3.5rem;
            }
        }
        .image-content{
            height: 425px;
            width: 100%;display:flex;
            justify-content:center;
            align-items:center;
            overflow: hidden
        }
        .image-content>img{
            width:100%;
            vertical-align: middle;
        }
    </style>
    <!-- Custom styles for this template -->
    <link href="/static/css/navbar.css" rel="stylesheet">
</head>
<body class="d-flex flex-column h-100">

<header>
    <!-- Fixed navbar -->
        <div class="navbar navbar-dark bg-dark shadow-sm">
            <div class="container d-flex justify-content-between">
                <a href="/" class="navbar-brand d-flex align-items-center">
                    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" aria-hidden="true" class="mr-2" viewBox="0 0 24 24" focusable="false"><path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"></path><circle cx="12" cy="13" r="4"></circle></svg>
                    <strong>白茶清欢短视频</strong>
                </a>
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="{{urlfor "HomeController.Index"}}">抖音无水印下载工具 <span class="sr-only">(current)</span></a>
                    </li>
                </ul>
            </div>
        </div>
</header>

<!-- Begin page content -->
<main role="main" class="flex-shrink-0">
    <div class="album py-5 bg-light">
        <div class="container">
            <div class="row">
                {{range .List}}
                <div class="col-md-4">
                    <div class="card mb-4 shadow-sm" style="height: 550px">
                        <div class="image-content">
                            <img src="{{.VideoCover}}" alt="{{.Nickname}}">
                        </div>

                        <div class="card-body">
                            <p class="card-text">{{.Desc}}.</p>
                            <div class="d-flex justify-content-between align-items-center">
                                <div class="btn-group">
                                    <button data-toggle="modal" data-target="#playVideoModal" data-title="{{.Nickname}}" data-play="{{.VideoPlayAddr}}" class="btn btn-sm btn-outline-secondary">播放</button>
                                    <a href="{{.VideoRawPlayAddr}}" target="_blank" class="btn btn-sm btn-outline-secondary">来源</a>
                                </div>
                                <small class="text-muted">{{dateformat .Created "2006-01-02 15:04:05"}}</small>
                            </div>
                        </div>
                    </div>
                </div>
                {{else}}
                   <p>没有数据</p>
                {{end}}
            </div>
            <div class="row">
                <nav aria-label="Page navigation example">
                    <ul class="pagination">
                        <li class="page-item{{if eq .Previous "#"}} disabled{{end}}"><a class="page-link" href="{{.Previous}}">上一页</a></li>
                        <li class="page-item{{if eq .Next "#"}} disabled{{end}}"><a class="page-link" href="{{.Next}}">下一页</a></li>
                    </ul>
                </nav>
            </div>
        </div>
    </div>
    <div class="modal fade" id="playVideoModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">New message</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div class="container-fluid">
                        <div class="row">
                            <video id="playVideo" autoplay="autoplay" width="100%" controls="controls">
                                您的浏览器不支持 video 标签。
                            </video>
                        </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">关闭</button>
                </div>
            </div>
        </div>
    </div>
</main>

<footer class="footer mt-auto py-3">
    <div class="container">
        <span class="text-muted">抖音无水印下载.</span>
    </div>
</footer>
<script src="/static/jquery/jquery-3.6.0.min.js"></script>
<script src="/static/bootstrap/v4.6.0/js/bootstrap.bundle.min.js"></script>
<script type="text/javascript">
    $(function () {
        $('#playVideoModal').on('show.bs.modal', function (event) {
            const button = $(event.relatedTarget);
            const title = button.data('title');
            const body = button.data('play')

            const modal = $(this);
            modal.find('.modal-title').text(title);
            modal.find('#playVideo').attr('src',body)
        });
        document.addEventListener('DOMContentLoaded', function () {
            function audioAutoPlay() {
                var audio = document.getElementById('playVideo');
                audio.play();
                document.addEventListener("WeixinJSBridgeReady", function () {
                    audio.play();
                }, false);
            }
            audioAutoPlay();
        });

        if (/i(Phone|P(o|a)d)/.test(navigator.userAgent)) {
            $(document).one('touchstart',
                function(e) {
                    $('#playVideo').get(0).touchstart = true;
                    $('#playVideo').get(0).play();
                    return false;
                });
        }
    });

</script>
</body>
</html>
